{% extends 'AppSkeletonCmsBundle::base.html.twig' %}

{% block breadcrumb %}
	<li>
		<a href="{{ path('attachments') }}">attachments</a> <span class="divider">/</span>
	</li>
	<li>
		<a href="#">new</a> <span class="divider">/</span>
	</li>
{% endblock %}

{% block body -%}
	<h1>Attachments creation</h1>

	<form action="{{ path('attachments_create') }}" method="post" {{ form_enctype(form) }}>
		{{ form_widget(form) }}
		<p>
			<button type="submit">Create</button>
		</p>
	</form>

	<ul class="record_actions">
		<li>
			<a href="{{ path('attachments') }}">
				Back to the list
			</a>
		</li>
	</ul>
{% endblock %}
