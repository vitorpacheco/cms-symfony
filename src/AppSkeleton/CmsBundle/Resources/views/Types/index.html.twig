{% extends '::base.html.twig' %}

{% block breadcrumb %}
	<li>
		<a href="#">types</a> <span class="divider">/</span>
	</li>
{% endblock %}

{% block body -%}
	<h1>Types list</h1>

	<table class="records_list">
		<thead>
		<tr>
			<th>Id</th>
			<th>Name</th>
			<th>Postcount</th>
			<th>Created</th>
			<th>Updated</th>
			<th>Actions</th>
		</tr>
		</thead>
		<tbody>
		{% for entity in entities %}
			<tr>
				<td><a href="{{ path('types_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
				<td>{{ entity.name }}</td>
				<td>{{ entity.postCount }}</td>
				<td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
				<td>{% if entity.updated %}{{ entity.updated|date('Y-m-d H:i:s') }}{% endif %}</td>
				<td>
					<ul>
						<li>
							<a href="{{ path('types_show', { 'id': entity.id }) }}">show</a>
						</li>
						<li>
							<a href="{{ path('types_edit', { 'id': entity.id }) }}">edit</a>
						</li>
					</ul>
				</td>
			</tr>
		{% endfor %}
		</tbody>
	</table>

	<ul>
		<li>
			<a href="{{ path('types_new') }}">
				Create a new entry
			</a>
		</li>
	</ul>
{% endblock %}
